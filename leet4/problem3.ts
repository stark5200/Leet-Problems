function canPlaceFlowers(flowerbed: number[], n: number): boolean {
    let can: boolean = true;
    let space: number = 0;
    let size = flowerbed.length;
    flowerbed[0] == 1 ? null : (flowerbed[1] == 1 ? null : space+=1);
    for (let i = 1; i < size; i++) {
      
    }
    return can;
};
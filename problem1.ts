function mergeAlternately(word1: string, word2: string): string {
(totallen: Number) => word1.length + word2.length;
(tl: String) => totallen.string;
return(word1+word2+String(totallen));
};

mergeAlternately("hi", "yummy");